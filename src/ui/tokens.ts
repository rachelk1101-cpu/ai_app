/**
 * 발달장애인 사용자 대상 UI 규칙을 코드에 상수로 고정한다.
 * 화면마다 다시 판단하지 않도록, 공용 컴포넌트가 이 값을 강제로 적용한다.
 */
export const A11Y = {
  /** 버튼 최소 높이(px). 손 떨림이 있어도 누를 수 있는 크기. */
  MIN_TOUCH_PX: 88,
  /** 본문 최소 글자 크기(px). */
  MIN_FONT_PX: 24,
  /** 화면 전환 최소 시간(ms). 갑작스러운 전환은 혼란을 준다. */
  TRANSITION_MS: 300,
  /** 한 화면에 두는 조작 가능한 요소의 최대 개수. */
  MAX_ACTIONS_PER_SCREEN: 2,
} as const
